should match snapshot:
  1: |
    apiVersion: v1
    data:
      config.toml: |
        # template
    kind: ConfigMap
    metadata:
      annotations:
        reloader.stakater.com/match: "true"
      name: RELEASE-NAME-podsync
